*.json
*/build/
/.vscode/
*.pyc
/build/
*~
.DS_Store
*.swp
.venv
.gdb_history
*/*.a
*/*.so

# IntelliJ IDEA
/.idea
**/*.iml
*.json
llvm_build_dirs

/llvm-project
/data
/patch
/pocs
/log/
/fuzzbench-output

/script/setenv.sh
# The self-contained dataset: per-target bug matrices, build maps, PoCs and
# OSV metadata. The blanket *.json above would otherwise drop the metadata.
!dataset/
!dataset/**
!dataset/*.json
